STYLE_SHEET="$(readlink -f "$SASS_DIR/../style.css")"

# Check if debug is set:
grep -i "^define.*WP_DEBUG.*true" ../../../../wp-config.php > /dev/null
if [[ $? == 0 ]] ; then
    FLAGS="-ml -t expanded"
else
    FLAGS="-ml -t compressed"
fi

# vim:set ft=sh:
